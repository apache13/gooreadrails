<ul class="books">
	<% @books.each do |book| %>
	<li class="book">
		<div class="thumbnail">
			<%= image_tag book.image , :size => '200x282' %>
		</div>
		<h1><%= book.title %></h1>
		<p class="detail">
			<%= book.description %>
		</p>
	</li>
	<% end %>
</ul>
